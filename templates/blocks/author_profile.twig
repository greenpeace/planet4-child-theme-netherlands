{% if ( post.author.name and post.author.description ) %}
	<div class="container clearfix gpnl-author">

		<h2 class="author-block-info-title">Auteur</h2>

		<footer class="author-block" itemtype="http://schema.org/Person">

			{% if ( post.author.avatar ) %}
				<figure class="gpnl-author-block-image d-none d-md-block">
					<img itemprop="image" class="author-pic"
						 src="{{ fn('get_avatar_url', post.author.id, {'size' : 294}) }}"
						 alt="{{ post.author.name }}">
				</figure>
			{% endif %}

			<div class="author-block-info">
				<h4 rel="author" class="author-block-info-name" itemprop="name">
					{{ post.author.name }}
				</h4>

				{# Get the tags from the post and iterate over them #}
				{% set tags = function('get_the_tags', post.ID) %}

				{% if post_tags %}
					{% for tag in tags %}
						<a class="tag-item" href="/nl/tag/{{ tag.slug }}"> #{{ tag.name|lower|raw }} </a>
					{% endfor %}
				{% endif %}

				<div class="author-block-info-bio" itemprop="description">
					{{ fn('wpautop', fn('the_author_meta', 'description', author.id))|e('wp_kses_post')|raw }}
				</div>




				<a href="{{ post.author.link }}" itemprop="name" class="read-more">Meer van {{ post.author.name }}</a>


			</div>

		</footer>
	</div>
{% endif %}
